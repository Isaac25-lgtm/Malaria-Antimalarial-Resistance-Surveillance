"""The synthetic demo dataset — Prompt 10.

A compelling presentation must never depend on live sensitive data. This
package generates a fictional but structurally realistic dataset with a small
number of deliberately planted storylines, so the whole national → district →
facility → signal → explanation journey can be walked safely.

The dataset is produced as **inbound-contract artefacts**, so it loads through
the same ingestion pipeline and the same validation as real data. See
``docs/demo/synthetic-dataset.md``.
"""

from __future__ import annotations
